.TH man 1 "07 sep 2022" "1.01" "simple_shell man page"

.SH NAME
.B simple_shell
- Simple shell made in C language to interact with a linux operating system.

.SH SYNOPSIS
$ /hsh [options] [file]

.SH DESCRIPTION
simple_shell is a command line interpreter that executes
commands read from the standard input.
Clone of sh

.SH Return Value

Always returns the status of the previously executed command( 0 if no command was executed) , number means exit failed codes

.SH PATH

Looks in $PATH env variable for executable program file location.

.SH

.SH OPTIONS
.B simple_shell
Command options will be typed after the main command followed by a space and a
 '-' symbol.

.SH EXAMPLE

./hsh [command] [options]
$ ls -l


.SH SEE ALSO
access(2), chdir(2), execve(2), _exit(2), exit(3), fflush(3), fork(2), free(3), isatty(3), getcwd(3), malloc(3), open(2), read(2), sh(1), signal(2), stat(2), wait(2), write(2)

This man page borrows from the corresponding man pages sh(1) and dash(1).

.SH AUTHOR
Claudio Kimani, Dominic Sengo
